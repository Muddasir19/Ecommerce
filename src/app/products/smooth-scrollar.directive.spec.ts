import { SmoothScrollarDirective } from './smooth-scrollar.directive';

describe('SmoothScrollarDirective', () => {
  it('should create an instance', () => {
    const directive = new SmoothScrollarDirective();
    expect(directive).toBeTruthy();
  });
});
